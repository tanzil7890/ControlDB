apiVersion: v1
kind: Service
metadata:
  name: {{ include "controldb.fullname" . }}
  labels:
    {{- include "controldb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "controldb.selectorLabels" . | nindent 4 }}
