apiVersion: v1
kind: Service
metadata:
  name: controldb
  namespace: controldb
  labels:
    app.kubernetes.io/name: controldb
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: controldb
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
